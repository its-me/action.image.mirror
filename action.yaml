name: Cross-registry image mirror
description: Copy each of a set of tags from one image repository to another via `docker buildx imagetools create`.
author: Sergey Kanafyev

branding:
  icon: copy
  color: blue

inputs:
  source:
    description: >
      Source image repository, e.g. "ghcr.io/its-me/workflow" or
      "1tsme/workflow". The registry to log in to is derived from the
      first path segment: it's treated as a hostname (and logged in to)
      when it contains a "." or ":" or is "localhost", otherwise the
      image is assumed to be on Docker Hub.
    required: true
  source-username:
    description: Username for logging in to the source registry.
    required: true
  source-password:
    description: Password or token for logging in to the source registry.
    required: true
  destination:
    description: >
      Destination image repository, e.g. "1tsme/workflow" or
      "quay.io/itsme/workflow". The registry to log in to is derived the
      same way as for "source".
    required: true
  destination-username:
    description: Username for logging in to the destination registry.
    required: true
  destination-password:
    description: Password or token for logging in to the destination registry.
    required: true
  tags:
    description: Whitespace-separated list of tag names to copy from source to destination.
    required: true

runs:
  using: composite
  steps:
    - name: Resolve source registry
      id: source-registry
      shell: bash
      env:
        SOURCE: ${{ inputs.source }}
      run: |
        first="${SOURCE%%/*}"
        if [[ "$first" == *.* || "$first" == *:* || "$first" == "localhost" ]]; then
          echo "value=$first" >> "$GITHUB_OUTPUT"
        else
          echo "value=" >> "$GITHUB_OUTPUT"
        fi

    - name: Resolve destination registry
      id: destination-registry
      shell: bash
      env:
        DESTINATION: ${{ inputs.destination }}
      run: |
        first="${DESTINATION%%/*}"
        if [[ "$first" == *.* || "$first" == *:* || "$first" == "localhost" ]]; then
          echo "value=$first" >> "$GITHUB_OUTPUT"
        else
          echo "value=" >> "$GITHUB_OUTPUT"
        fi

    - name: Log in to source registry
      uses: docker/login-action@06fb636fac595d6fb4b28a5dfcb21a6f5091859c # v4.5.0
      with:
        registry: ${{ steps.source-registry.outputs.value }}
        username: ${{ inputs.source-username }}
        password: ${{ inputs.source-password }}

    - name: Log in to destination registry
      uses: docker/login-action@06fb636fac595d6fb4b28a5dfcb21a6f5091859c # v4.5.0
      with:
        registry: ${{ steps.destination-registry.outputs.value }}
        username: ${{ inputs.destination-username }}
        password: ${{ inputs.destination-password }}

    - name: Mirror image
      shell: bash
      env:
        SOURCE: ${{ inputs.source }}
        DESTINATION: ${{ inputs.destination }}
        TAGS: ${{ inputs.tags }}
      run: |
        for tag in $TAGS; do
          docker buildx imagetools create \
            --tag "$DESTINATION:$tag" \
            "$SOURCE:$tag"
        done
